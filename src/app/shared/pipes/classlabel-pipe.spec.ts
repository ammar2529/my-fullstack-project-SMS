import { ClasslabelPipe } from './classlabel-pipe';

describe('ClasslabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasslabelPipe();
    expect(pipe).toBeTruthy();
  });
});
